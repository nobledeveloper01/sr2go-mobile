# Copy to .env. The .env file is gitignored and must never be committed.
EXPO_PUBLIC_API_BASE_URL=https://shareride2go.com

# The reviewer's test account. Kept out of the repo on purpose: credentials
# belong in an environment file, not in source control, even test ones.
EXPO_PUBLIC_DEMO_EMAIL=
EXPO_PUBLIC_DEMO_PASSWORD=
